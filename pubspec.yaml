name: astMacro
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: gordianknot <email@example.com>

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  petitparser: ^2.0.2    #PetitParser for Dart
  quiver: ^2.0.0+1  #collection utilities
  yaml: ^2.1.15   #yaml
  colorize: ^2.0.0    #support colorizing console color
  IO:
    path: ../IO
  common:
    path: ../common
dev_dependencies:
  test: ^1.0.0
